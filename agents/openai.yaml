interface:
  display_name: "/xxd-panel-029"
  short_description: "Create Panel 029 from its original style brief."
  default_prompt: "Use $xxd-panel-029; keep its bundled original style brief as the sole aesthetic authority, use real multi-select only when the host exposes it, use single-choice tools only for single-choice fields, otherwise use two-round combination input, and ask only for unresolved settings."
